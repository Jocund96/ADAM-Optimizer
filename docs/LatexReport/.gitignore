# Ignore everything in this folder
*

# Keep all subdirectories so git can inspect them
!*/

# Whitelist compiled PDF files
!*.pdf

# Explicitly ensure .gitignore itself is tracked or untracked as needed
!.gitignore
